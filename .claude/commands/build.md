Build SimpleFlow project.

1. Create build directory if not exists
2. Run cmake configuration
3. Build with cmake --build
4. Report success or errors

Use PowerShell commands. Build directory: build/

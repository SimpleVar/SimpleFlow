Run SimpleFlow tests.

1. Ensure project is built (run /build if needed)
2. Execute ctest in build directory
3. Report test results (pass/fail count)

Use PowerShell commands. Build directory: build/
